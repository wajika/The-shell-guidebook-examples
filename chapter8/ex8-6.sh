#-----------------------------/chapter8/ex8-6.sh------------------
#! /bin/bash

#筛选所有以字符r开头，并且紧跟着1个字符c的文本行
str=`ls /etc |grep "^rc"`

echo "$str"

echo "=============================="

#筛选所有以字符r开头，紧跟着1个字符为c，下面1个字符为单个数字的文本行
str=`ls /etc | grep "^rc[0-9]"`

echo "$str"
